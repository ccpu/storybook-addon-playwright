import { useStoryScreenshots } from '../use-story-screenshots';

describe('useStoryScreenshots', () => {
  it('should load data', () => {
    //
  });

  it('should handle error', () => {
    //
  });
});
